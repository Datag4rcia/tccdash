git add requirements.txt
   git commit -m "Add requirements.txt"
   git push
```

4. **O Streamlit Cloud vai reinstalar** automaticamente com as dependências corretas

## 📁 Estrutura do seu repositório deve ficar assim:
```
seu-repositorio/
├── app.py
├── requirements.txt
└── meus_dados.csv